<%= simple_form_for([@play, @play.reviews.new]) do |f| %>
  <div id="rating-form">
    <%= f.input :rating %>
      <%= f.input :comment %>
      <%= f.button :submit, class: "btn-custom2"%>
    </div>
<% end %>